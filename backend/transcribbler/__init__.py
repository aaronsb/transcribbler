"""transcribbler compute backend.

A thin orchestrator over swappable inference engines ("cores"), selected by a
declarative compute profile (ADR-0015), producing Canonical IR (ADR-0006).
"""

__version__ = "0.1.0"
