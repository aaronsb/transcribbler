# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build artifacts (make dist / uv build)
dist/
build/

# Rust
target/

# Models & weights (never commit these)
models/
*.gguf
*.bin
*.pt
*.onnx
*.ckpt

# Audio / captured data (privacy: never commit recordings or transcripts)
*.wav
*.mp3
*.m4a
*.flac
*.ogg
*.mkv
*.mp4
captures/
transcripts/
*.vtt

# Secrets / local config
.env
.env.*
!.env.example
secrets/
*.token

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# legacy live-capture output written to CWD (pre-XDG); new runs go to XDG data/state
transcript-*.md
.transcript-*.capture/
*.ir.json
